<UserControl x:Class="CashierRegister.UserControls.UCButtonOnOff"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">

    <Border CornerRadius="3" VerticalAlignment="Top" BorderThickness="1" BorderBrush="#FF004D40" Height="25" Margin="10 5 0 0" Width="80" HorizontalAlignment="Left">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Border x:Name="bdOff" BorderThickness="1" CornerRadius="3" BorderBrush="Transparent" Background="#FF004D40">
                <TextBlock x:Name="tblOff" Text="Off" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Medium"></TextBlock>
            </Border>

            <Border x:Name="bdOn" Grid.Column="1" BorderThickness="1" CornerRadius="3" BorderBrush="Transparent">
                <TextBlock x:Name="tblOn" Text="On" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Silver" FontWeight="Medium"></TextBlock>
            </Border>
        </Grid>
    </Border>
</UserControl>
